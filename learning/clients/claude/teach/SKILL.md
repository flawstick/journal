---
name: teach
description: Teach, explain, practise, review, or continue learning any topic using the shared learning vault.
---

Use the native folder tools to connect the complete repository `/Users/edo/dev/python/journal` and vault `/Users/edo/Library/Mobile Documents/iCloud~md~obsidian/Documents/the-vault`. Read `learning/skills/learn/SKILL.md` from the repository and follow it: this is the shared, current tutoring workflow.

In a mounted workspace, run the repository’s `learning/skills/learn/scripts/learn` with `VAULT_DIR` set to the actual vault mount path. Use the mounted paths for file access; do not assume the command runs on macOS. Keep lookup and record keeping silent. Respond only with study content focused on the current question.
